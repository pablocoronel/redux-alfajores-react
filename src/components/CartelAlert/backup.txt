shouldComponentUpdate(nextProps, nextState) {
		// console.log(nextProps);
		return nextProps.response.type !== this.props.response.type;
	}
	componentWillUpdate(nextProps, nextState) {
		console.log(nextProps);
		let mostrar = '';
		let bsAlert = '';
		// console.log(this.props)
		if (nextProps.response.type === 'success') {
			console.log('UPDATE SUCCESS');
			mostrar = 'block';
			bsAlert = 'success';
			this.setState({
				styleMostrar: 'block',
				styleAlert: 'success'
			});
			// console.log('ACA');
		} else if (nextProps.response.type === 'error') {
			// Object.keys(this.props.response).map((key, index) => {
			// 	console.log(key);
			// 	console.log(this.props.response[key]);
			// });
			console.log('UPDATE ERROR');
			mostrar = 'block';
			bsAlert = 'danger';
			this.setState({
				styleMostrar: 'block',
				styleAlert: 'danger'
			});
		} else {
			mostrar = 'ff';
			bsAlert = 'eee';
		}

		// this.setState({
		// 	styleMostrar: mostrar,
		// 	styleAlert: bsAlert
		// });
		// console.log(this.state)
	}

	componentWillMount() {
		let mostrar = '';
		let bsAlert = '';
		console.log(this.props);
		if (this.props.response.type === 'success') {
			mostrar = 'block';
			bsAlert = 'success';
			this.setState({
				styleMostrar: 'block',
				styleAlert: 'success'
			});
			console.log('MOUNT SUCCESS');
		} else if (this.props.response.type === 'error') {
			// Object.keys(this.props.response).map((key, index) => {
			// 	console.log(key);
			// 	console.log(this.props.response[key]);
			// });
			console.log('MOUNT ERROR');
			mostrar = 'block';
			bsAlert = 'danger';
			this.setState({
				styleMostrar: 'block',
				styleAlert: 'danger'
			});
		} else {
			mostrar = 'none';
			bsAlert = 'warning';
		}
		// console.log(bsAlert)
		// this.setState({
		// 	styleMostrar: mostrar,
		// 	styleAlert: bsAlert
		// });
	}